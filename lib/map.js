var GEOCODE_URL = 'https://maps.googleapis.com/maps/api/geocode/json';
var requestCount = 0;
var totalCount = 0;

module.exports = {
    geocodeAddresses: function(addressList) {
        totalCount = addressList.length * 2;
        _(addressList).forEach(userObject) {
            
        }).value();
    },
    geocodeAddress: function() {
        // finish query params, etc
        queryString = { address: userObject
        request({ url:     }, function(error, response, body) {
            requestCount++;

            if (requestCount = 

        }
    },
    doneGeocoding: function(filledAddressBlob) {

    }
}
